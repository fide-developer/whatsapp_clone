import styled from "styled-components";
import { FlexHorizontalContainer } from "../../../GlobalUses/GlobalStyles";

export const IconContainer = styled(FlexHorizontalContainer)`
    flex-wrap: wrap;

    width: inherit;

    span{
        padding: 8px 4px;
        flex-shrink: 0;
        font-size: 18pt;
    }
`