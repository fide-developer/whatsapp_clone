import { url } from 'inspector'
import styled from 'styled-components'
import { FlexHorzContainer, FlexVertContainer } from "../../GlobalUses/GlobalStyles"

export const ListMessage = styled(FlexVertContainer)`
    padding: 0;
    width: 100%;
    flex-grow: 1;
    overflow-y: auto;
`

