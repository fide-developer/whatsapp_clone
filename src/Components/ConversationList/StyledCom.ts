import { url } from 'inspector'
import styled from 'styled-components'
import { FlexVerticalContainer } from "../../GlobalUses/GlobalStyles"

export const ListMessage = styled(FlexVerticalContainer)`
    padding: 0;
    width: 100%;
    flex-grow: 1;
    overflow-y: auto;
`

